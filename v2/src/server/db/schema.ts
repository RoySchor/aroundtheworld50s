// Drizzle schema lives here.
// Phase 2 will define: users, blog_posts, blog_blocks, tips, tip_sections, gallery_images.
// Keep this file as the single source of truth for the database shape —
// migrations are generated from it via `npm run db:generate`.

export {};
